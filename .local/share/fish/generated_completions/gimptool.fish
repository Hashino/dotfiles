# gimptool
# Autogenerated from man page /usr/share/man/man1/gimptool.1.gz
complete -c gimptool -l version -d 'Print the currently installed version of GIMP on the standard output'
complete -c gimptool -l help -d 'Print out the help blurb, showing commonly used commandline options'
complete -c gimptool -l quiet -d 'Run quietly without echoing any of the build commands'
complete -c gimptool -l silent -d 'Run silently without echoing any of the build commands.  Same as --quiet'
complete -c gimptool -s n -d 'Test mode.  Print the commands but don\'t actually execute them'
complete -c gimptool -l just-print -d 'Test mode.  Print the commands but don\'t actually execute them.  Same as -n'
complete -c gimptool -l dry-run -d 'Test mode.  Print the commands but don\'t actually execute them.  Same as -n'
complete -c gimptool -l recon -d 'Test mode.  Print the commands but don\'t actually execute them.  Same as -n'
complete -c gimptool -l msvc-syntax -d 'Useful on Windows'
complete -c gimptool -l bindir -d 'Outputs the bindir used to install the GIMP'
complete -c gimptool -l sbindir -d 'Outputs the sbindir used to install the GIMP'
complete -c gimptool -l libexecdir -d 'Outputs the libexecdir used to install the GIMP'
complete -c gimptool -l datadir -d 'Outputs the datadir used to install the GIMP'
complete -c gimptool -l sysconfdir -d 'Outputs the sysconfdir used to install the GIMP'
complete -c gimptool -l sharedstatedir -d 'Outputs the sharedstatedir used to install the GIMP'
complete -c gimptool -l localstatedir -d 'Outputs the localstatedir used to install the GIMP'
complete -c gimptool -l libdir -d 'Outputs the libdir used to install the GIMP'
complete -c gimptool -l infodir -d 'Outputs the infodir used to install the GIMP'
complete -c gimptool -l mandir -d 'Outputs the mandir used to install the GIMP'
complete -c gimptool -l includedir -d 'Outputs the includedir used to install the GIMP'
complete -c gimptool -l gimpdatadir -d 'Outputs the actual directory where the GIMP data files were installed'
complete -c gimptool -l gimpplugindir -d 'Outputs the actual directory where the GIMP plug-ins were installed'
complete -c gimptool -l build -d 'Compile and link plug-in. c into a GIMP plug-in'
complete -c gimptool -l build-strip -d 'Compile,link, and strip plug-in. c into a GIMP plug-in'
complete -c gimptool -l install -d 'Compile, link, and install plug-in'
complete -c gimptool -l install-strip -d 'Compile, link,strip, and install plug-in'
complete -c gimptool -l install-admin -d 'Compile, link, and install plug-in'
complete -c gimptool -l install-bin -d 'Install plug-in into the user\'s personal GIMP plug-in directory ($XDG_CONFIG_…'
complete -c gimptool -l install-admin-bin -d 'Install plug-in into the system-wide GIMP plug-in directory (${exec_prefix}/l…'
complete -c gimptool -l install-bin-strip -d 'Install stripped plug-in into the user\'s personal GIMP plug-in directory ($XD…'
complete -c gimptool -l install-admin-bin-strip -d 'Install stripped plug-in into the system-wide GIMP plug-in directory (${exec_…'
complete -c gimptool -l install-script -d 'Install script'
complete -c gimptool -l install-admin-script -d 'Install script'
complete -c gimptool -l uninstall-bin -d 'Uninstall plug-in from the user\'s personal GIMP plug-in directory ($XDG_CONFI…'
complete -c gimptool -l uninstall-admin-bin -d 'Uninstall plug-in from the system-wide GIMP plug-in directory (${exec_prefix}…'
complete -c gimptool -l uninstall-script -d 'Uninstall script'
complete -c gimptool -l uninstall-admin-script -d 'Uninstall script'
complete -c gimptool -l libs -d 'Print the linker flags that are necessary to link a GIMP plug-in'
complete -c gimptool -l libs-noui -d 'Print the linker flags that are necessary to link a GIMP plug-in, for plug-in…'
complete -c gimptool -l cflags -d 'Print the compiler flags that are necessary to compile a GIMP plug-in'
complete -c gimptool -l clags-noui -d 'Print the compiler flags that are necessary to compile a GIMP plug-in for plu…'
complete -c gimptool -l prefix -d 'If specified, use PREFIX instead of the installation prefix that GIMP was bui…'
complete -c gimptool -l exec-prefix -d 'If specified, use PREFIX instead of the installation exec prefix that GIMP wa…'

